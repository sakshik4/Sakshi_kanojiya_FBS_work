#include<stdio.h>
void main()
{
	int arr[6];
	printf("Enter 6 elements");
	for(int i=0;i<6;i++){
	scanf("%d",&arr);
}
	for(int i=0;i<6;i++)
	{
		if(arr[i]<=1)
		{
			if(arr[i] % i == 0)
		}
		 printf("Prime in numbers in array are\n %d ",arr[i]);
	}
}
}